### Redis 
- Master runs on node-3
- Replicas on node-1 & node-2 as failovers
- Writes every second from
- Snapshots periodically
- Memory limited to 500M, which should limit storage to roughly the same
