apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: juicefs-test
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: juicefs-sc
  resources:
    requests:
      storage: 1Gi
